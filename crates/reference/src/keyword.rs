#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Keyword {
  Else,
  If,
  While,
  For,
  Loop,
  Gen,
  Let,
  Const,
  Return,
  Break,
  Continue,
  Match,
  Fn,
  Use,
  In,
  And,
  Or,
  Not,
}
